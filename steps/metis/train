#!/usr/bin/env bash
# metis/train — step-type wrapper (see steps/metis/cv-split for the contract note).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../.." && pwd)"
exec uv run --project "$ROOT" python -m metis.steps.train
